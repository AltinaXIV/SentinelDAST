// Main entry point for the application
// For WPF apps, this file is not typically needed as App.xaml handles startup
// Keeping this file as a reference or for potential console mode operations

using SentinelDAST;

// Force use of the WPF entry point via App.xaml
var app = new App();
app.InitializeComponent();
app.Run();
